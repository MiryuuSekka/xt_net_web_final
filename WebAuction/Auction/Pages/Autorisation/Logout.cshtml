@using Auction.Code;
@{
    Layout = null;

    var code = new Auction.Code.Logic();
    Logic.CurrentUser = code.Code.GetUserById(2);
    FormsAuthentication.SignOut();

    Response.Redirect("~/Pages/Index.cshtml");
}
