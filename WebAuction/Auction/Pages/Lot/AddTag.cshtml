@using Auction.Code;
@using Entity.Classes;
@{
    Layout = null;

    var id = Request["id"];
    int.TryParse(id, out int Id);

    var tag = Request["value"];

    var code = new Logic();
    var lot = code.Code.GetLotById(Id);

    //code.Code.AddTag();

    Response.Redirect("~/Pages/Lot/View.cshtml?id=" + id);
}