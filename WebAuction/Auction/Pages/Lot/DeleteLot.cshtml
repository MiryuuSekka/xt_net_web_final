@using Entity.Classes;
@using Auction.Code;

@{
    Layout = null;

    var code = new Logic();
    var LotId = Request["id"];
    int.TryParse(LotId, out int id);
    code.Code.DeleteLot(id);
    
    Response.Redirect("~/Pages/Index.cshtml");
}